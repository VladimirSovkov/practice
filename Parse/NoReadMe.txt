using Parse.Domain.Model;
using Parse.Infrastructure;
using System;
using System.Collections.Generic;
using System.Linq;

namespace Parse
{
    class Program
    {
        //static void Main(string[] args)
        //{
        //    //ParseXML xmlData = new ParseXML();

        //    //IEnumerable<XMLModel> xml = xmlData.GetData("https://www.nationalbank.kz/rss/get_rates.cfm?fdate=18.07.2020");

        //    ParseJSON jsonData = new ParseJSON();
        //    IEnumerable<JSONModel> jsons = jsonData.GetData("https://bank.gov.ua/NBUStatService/v1/statdirectory/exchange?date=20190320&json");
        //    List<JSONModel> abc = jsons.ToList();
        //    string dateStr = abc[1].exchangedate;
        //    DateTime date = Convert.ToDateTime(dateStr);
        //    int stop = 0;

        //}
    }
}